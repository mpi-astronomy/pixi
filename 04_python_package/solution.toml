[project]
authors = [{name = "TheSkyentist", email = "raphael.hviding@gmail.com"}]
dependencies = []
name = "fastadd"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["osx-arm64"]

[tool.pixi.pypi-dependencies]
fastadd = { path = ".", editable = true }

[tool.pixi.tasks]
